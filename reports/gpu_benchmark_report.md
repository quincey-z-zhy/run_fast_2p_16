# GPU 吞吐基准报告

生成时间：2026-04-15
阶段：Stage 5（GPU 环境验证）
项目：run_fast_2p_16_v0

---

## 测试结论

**结论：GPU 吞吐满足 PPO 训练需求。**
- 神经网络推理 GPU 速度为 CPU 的 **6.6 倍**
- 48 GB 显存，小型 Actor-Critic MLP 仅占用 10.3 MB，可支持大批量并发训练
- 环境仿真（CPU）5,104 steps/sec，与本地一致（差异 < 3%）

---

## 硬件配置

| 项目 | 配置 |
|------|------|
| GPU 型号 | NVIDIA RTX5880-Ada-48Q |
| GPU 显存 | 48 GB |
| CPU 核心数 | 64 |
| 磁盘空间 | 441 GB 可用 |
| CUDA | Available |
| PyTorch | 2.11.0+cu128 |

---

## 基准测试结果

### 1. 环境仿真吞吐（CPU）

| 指标 | 数值 |
|------|------|
| steps/sec | **5,104** |
| 测试局数 | 927 局（5 秒内）|
| 本地对比 | 5,252 steps/sec（差异 2.8%，正常波动）|
| 评估 | ✅ 与本地一致 |

### 2. MLP 推理吞吐（Actor-Critic 网络）

网络结构：67→256→ReLU→256→ReLU → [Actor(270), Critic(1)]（标准 PPO Actor-Critic）

| 设备 | 延迟（ms/batch）| 吞吐（inferences/sec）| 备注 |
|------|----------------|----------------------|------|
| CPU | 0.542 ms | 944,567 | batch_size=512 |
| GPU | 0.083 ms | 6,199,904 | batch_size=512 |
| **GPU vs CPU 加速比** | **6.6x** | | |

### 3. GPU 显存占用

| 指标 | 数值 |
|------|------|
| 模型显存占用 | 10.3 MB |
| 已预留显存 | 25.2 MB |
| 可用显存 | ~48,000 MB |
| **显存利用率** | **< 0.1%**（极充裕）|

---

## PPO 训练能力预估

基于以上数据：

| 训练配置 | 预估吞吐 |
|---------|---------|
| 单进程（1 worker）| ~5,000 steps/sec |
| 多进程（8 workers）| ~40,000 steps/sec |
| 多进程（32 workers）| ~160,000 steps/sec |
| GPU batch update | 6.6x 快于 CPU 更新 |

训练 10M steps（常规基线训练量）预估耗时：
- 1 worker：~33 分钟
- 8 workers：~4 分钟
- 32 workers：~1 分钟

**结论：64 核 CPU + 48GB GPU 机器完全满足 PPO 自博弈训练需求，训练速度充裕。**

---

## 与阶段要求对比

| 门控要求 | 要求 | 实测 | 通过 |
|---------|------|------|------|
| GPU 环境可用 | CUDA available | ✅ | ✅ |
| 神经网络 GPU 加速 | >1x | 6.6x | ✅ |
| 显存充足 | 训练可运行 | 48GB，占用 <0.1% | ✅ |
| 环境仿真一致 | 与本地误差 <10% | 2.8% | ✅ |
